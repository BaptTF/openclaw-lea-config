# === Domains (set DNS A records to VPS IP) ===
QUIZ_DOMAIN=quiz.bapttf.com
POTAGER_DOMAIN=potager.bapttf.com
ACME_EMAIL=rene.jullien@gmail.com

# === GitHub OAuth (for potager auth) ===
# Create at: github.com/settings/developers > OAuth Apps
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
OAUTH_COOKIE_SECRET=  # Generate with: openssl rand -hex 16
